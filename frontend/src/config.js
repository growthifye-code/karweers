// Obscured admin console path (security-through-obscurity — real protection is the
// login + admin role). Must stay in sync with backend ADMIN_PATH in server.py.
export const ADMIN_PATH = "/sk-control-92f4a7e1";
